<%= form_for ([@question, @question.comments.build ]) do |f| %>

  <p> Text:</br> <%= f.text_field :body%> </p>

  <%= f.submit :Submit %>
<% end %>

<% if @comment.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@comment.errors.count, "errors") %> prohibited this article from being saved:
    </h2>
    <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
